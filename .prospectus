item do
  expected do
    url_xpath
    url 'https://gitlab.com/procps-ng/procps/tags'
    xpath '//ul[@class="content-list"]/li[1]/div[1]/a/strong/text()'
    regex /v([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
